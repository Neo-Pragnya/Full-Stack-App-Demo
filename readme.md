I want to create an demo of front end developed in Angular - that has a chat interface with thumbs down and Up - which takes inputs from user for their date of birth and name to compute their numerology and personality attributes. 

So the chat interface should have thumbs up and thumbs down - also other capabilities to click on other tabs - where each tab should their special numbers. All these clicks/messages should be captured as events in backend that should be sent as events - so design the angular frontend to send the messages to the backend for each of the events 

So these messages should be read by endpoint on backend written in fastAPI using python - try to orchestrate this as two different services that have to be started seperately for testing - but would be integrated using docker compose file. 

Make sure to test out the entire app - show how to start front end and backend. Show how to start docker_compose and all the things. Create detailed writeup for explaining the app and its operations - add sphinx docs for the project. 